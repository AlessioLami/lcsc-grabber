# LCSC Grabber Requirements
# Note: When running inside KiCad, wxPython is already available
# These are only needed for development/testing outside KiCad

# wxPython for GUI (bundled with KiCad)
# wxPython>=4.0.0

# easyeda2kicad for component conversion
easyeda2kicad>=0.8.0
